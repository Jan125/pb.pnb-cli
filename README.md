```
This program takes a file as input and executes it, or evaluates the given command line expression.
If you execute it without parameters, it goes into CLI mode. You can enter expressions freely.
Typing Exit inside of CLI mode exits CLI mode.

This application requires pnb.dll to be present inside the folder.

Please see pb.pnb for language specification.
